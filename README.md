
  # Create new design element

  This is a code bundle for Create new design element. The original project is available at https://www.figma.com/design/LXbDIa7XrtPfaBVfo7P7MF/Create-new-design-element.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  